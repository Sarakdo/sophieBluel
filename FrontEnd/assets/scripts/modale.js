// gestion de la modale creation et suppression

const modale_create_delete = () => {
  const update_projects = document.querySelector(".update_projects");
  const update_banner = document.querySelector(".banner_edit_mode");

  update_projects.addEventListener("click", () => {});

  createWorks();
};
